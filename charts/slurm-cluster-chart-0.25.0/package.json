{
  "name": "slurm-cluster-chart",
  "version": "0.25.0",
  "description": "openinnovation oi-slurm-cluster-chart helm chart",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "helm lint"
  },
  "keywords": [
    "helm",
    "slurm-cluster-chart",
    "openinnovation"
  ],
  "author": "openinnovation.ai",
  "license": "ISC",
  "devDependencies": {
    "@release-it/keep-a-changelog": "^5.0.0",
    "release-it": "^17.1.1"
  },
  "release-it": {
    "git": {
      "commitMessage": "chore: release slurm-cluster-chart-${version}",
      "tagName": "slurm-cluster-chart-${version}"
    },
    "gitlab": {
      "release": true
    },
    "npm": {
      "publish": false,
      "skipChecks": true
    }
  }
}
